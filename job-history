主要功能開發
1月 — 籌碼設置系統（從零開始）
新增籌碼設置 UI（Mobile/PC 雙版本）
拖曳交換功能 + ghost 動畫效果
籌碼金額 input 格式化、font-size 自適應
引入 vfm（vue-final-modal）做 Dialog 底層架構
建立 GlobalDialogPanelLayout 共用 Panel 基底
加入直橫式鎖屏 UI、vueuse 套件
2月 — 財務查詢 & 好路投注初版
手機版財務查帳 Dialog（由下往上進場）
影片回放功能
好路提示 Dialog
好路投注 Dialog 初版（含免佣、鎖定桌台）
Dialog 滑動關閉、進退場 Transition 優化
Menu 加上人數/桌台顯示、版本號
3月 — UI 精修 & 音效整合
全站各 Dialog 音效整合（好路/系統設置/財務/聊天室/籌碼）
好路投注介面細節調整（Loading、過濾、詳解）
影片回播介面重做、進度條直橫版判斷
財務報表 Calendar 樣式調整
聊天室介面調整
語系 Picker 樣式優化
4月 — Bug 修復密集衝刺
修復 45 個 Issue（#161, #175, #202, #226, #252, #295, #296, #297, #320, #337, #349, #350, #358, #367, #371, #378, #381, #390, #391, #393, #400, #402, #419, #426, #427, #446, #450, #451, #453, #465, #476, #479, #481, #482, #509, #515 等）
聊天室 emoji URL 修正、訊息捲動至最新
財務報表日期選擇器優化（可顯示 6 行）
Video Player 點擊畫面播放功能
深淺模式設定新增
總結： 四個月共 467 次 commit，完整交付了籌碼設置系統、財務查詢、好路投注三大功能模組，並在四月密集修復超過 45 個 Issue。


2025/04/20

1. 滿版的 loading 和局部的 loading 差別
2. 修正 design review 時，會有 1px 誤差
3. Dialog 的一些必要功能
4. 不同的套件怎麼選擇
5. 專案打包時的路徑，圖片加 hash 作用是為了避免快取，可以用到 vite.config.ts
6. 聊天室的圖破圖
7. 如果當有問題的時候就跟 QA 討論
    1. 選線時，在單台裡面是固定位置，在多台就是不固定的位置
8. 自適應的技巧
    1. 不同的裝置要計算 html font base
9. 循環列表
    1. 實作方式，透過 transform 去做或是整塊一起移動
10. 循環列表小計總計
11. 當外層定義 ref 然後他把值傳入，接下來內部可以一直響應這一個值嗎
12. 自己做一個播放器
13. 多語系破版問題，不同的語系的 CSS


2026/06/29
實作會員報表 和額度紀錄

在查帳那邊，通用版的查帳是舊會員都是北京時間 新會員可以是北京或是美東時間

通常來說 props 傳入跟用 proved inject 的差別，從查帳 dialog 一路傳到 searchBar 在傳入到 select 下拉式選單這邊

Css :is :where selector 對應到 tailwind 的 group 和 peer 用法

查帳的內容 要查通用版

籌碼的內容，

Tailwind 撰寫的時候 比較習慣寫在 template 還是寫在 class 裡面


2026/06/30
籌碼 sig 太多的話會造成 drag 事件會 lag 可以的話看是不是把它換成 base64 或是圖片

Select 他的型別可以是數字或字串 那這個型別可以定義成一個 type  
export type SelectOptionValue = string | number;

export interface SelectOption {
    text: string;
    value: SelectOptionValue;
}

pixel 都是


2026/07/01
深色/淺色模式實作完整報告
在這個 Vue 專案中，深色/淺色模式的實作是通過以下方式進行的：
1. 主題狀態存儲位置
檔案: src/bundle/common/ts/useRootStore.ts
主題狀態存儲在全局響應式對象 rootData 中：

/** 主題 */
export enum ThemeMode {
    NONE = "",
    /** 淺色 */
    LIGHT = "light",
    /** 深色 */
    DARK = "dark",
}

export const rootData = reactive({
    // ... 其他屬性
    // 深色模式 預留位 'light' 'dark' ''
    themeMode: ThemeMode.NONE,
    // ... 其他屬性
});

2. 切換主題函式位置與實現
主要切換函式: src/bundle/common/ts/utils/Utils.ts (第 868-879 行)

/**
 * 設定當前顏色模式
 * @param theme
 */
public static setTheme(theme: ThemeMode): void {
    // 先移除舊的
    if (rootData.themeMode != ThemeMode.NONE) {
        document.documentElement.classList.remove(rootData.themeMode);
    }

    // 設定預設theme :預設為空
    document.documentElement.setAttribute("data-theme", theme);
    document.documentElement.classList.add(theme);

    rootData.themeMode = theme;
}
Composable 封裝: src/bundle/common/ts/hook/useSystemSetting.ts

const currentTheme = computed({
    get: () => rootData.themeMode,
    set: (mode: ThemeMode) => Utils.setTheme(mode),
});
UI 切換元件:
* src/bundle/common/vue/components/panel/systemSetting/ThemeModeRadioGroup.vue - 淺色/深色模式選項按鈕
* src/bundle/common/vue/components/dialogs/global/DialogMobileSystemSetting.vue - 手機系統設定面板
* src/bundle/common/vue/components/TestCodePanel.vue - 測試用面板（第 11-12 行）

<!-- TestCodePanel.vue 中的切換按鈕 -->
<button class="btn" :class="[deviceClass, { current: rootData.themeMode === ThemeMode.LIGHT }]" @click="toggleTheme">
    {{ rootData.themeMode === ThemeMode.LIGHT ? "淺色模式" : "深色模式" }}
</button>
切換函式：

function toggleTheme() {
    const newTheme = rootData.themeMode === ThemeMode.LIGHT ? ThemeMode.DARK : ThemeMode.LIGHT;
    Utils.setTheme(newTheme);
}

3. CSS 實現層面
a) HTML 根元素的 class 和 data-theme 屬性
當 Utils.setTheme() 被呼叫時，會：
* 在 <html> 根元素上添加 class: "light" 或 "dark"
* 設定 data-theme 屬性: <html class="light" data-theme="light">
b) CSS 變數定義
檔案: src/theme/tailwind-tokens-output.css
深色模式 (預設) - 第 100-759 行：

/* ===== 色彩模式 - 深色（預設）===== */
--color-common-bottom-hint: var(--tw-color-gold300);
--color-common-chip-setting: var(--tw-color-gold100);
--tw-color-common-bg-content: var(--tw-color-gold800);
/* ... 更多深色色彩變數 */
淺色模式 - 第 1762-2100+ 行：

/* 色彩模式 - 淺色 */
:root.light {
    --tw-color-common-bottom-hint: var(--tw-color-light-blue-gray300);
    --tw-color-common-chip-setting: var(--tw-color-light-blue-gray500);
    --tw-color-common-bg-content: var(--tw-color-light-blue-gray200);
    /* ... 更多淺色色彩變數 */
}
c) Tailwind CSS 整合
檔案: src/theme/tailwind.css
專案使用 Tailwind CSS 作為主要樣式框架，並透過自定義 CSS 變數前綴 tw: 使用顏色變數。
例如：

<div :class="[deviceClass, rootData.themeMode]"></div>

4. 主題初始化流程
檔案: src/main/ts/useRootView.ts (第 687 行)
主題在用戶登入後從後端配置初始化：

// 深淺模式：套用 data-theme 到 html 根元素（由 query 參數 ?theme=dark|light 決定）
Utils.setTheme(bgInfo.theme === "light" ? ThemeMode.LIGHT : ThemeMode.DARK);
初始化位置: 在 RootView 類別的數據初始化時，根據後端返回的 bgInfo.theme 參數自動設定主題。

5. 相關檔案路徑完整清單
文件路徑	用途
src/bundle/common/ts/useRootStore.ts	主題狀態定義與全局數據存儲
src/bundle/common/ts/utils/Utils.ts	setTheme() 核心方法
src/bundle/common/ts/hook/useSystemSetting.ts	currentTheme computed 屬性
src/bundle/common/vue/components/panel/systemSetting/ThemeModeRadioGroup.vue	主題選擇單選按鈕組件
src/bundle/common/vue/components/dialogs/global/DialogMobileSystemSetting.vue	手機系統設定對話框
src/bundle/common/vue/components/TestCodePanel.vue	測試用深淺色切換面板
src/theme/tailwind-tokens-output.css	CSS 變數定義（深色/淺色）
src/theme/tailwind.css	Tailwind CSS 設定
src/theme/global.css	全局樣式入口
src/main/ts/useRootView.ts	主題初始化邏輯
src/main/main.ts	應用入口
src/main/vue/App.vue	應用根組件
index.html	HTML 根元素（應用掛載點）
6. 運作流程總結

初始化流程：
┌─────────────────────────────────────────────────┐
│ 1. useRootView 初始化                           │
│    └─ bgInfo.theme 從後端讀取                   │
│                                                  │
│ 2. Utils.setTheme() 被調用                      │
│    └─ document.documentElement.classList.add()  │
│    └─ document.documentElement.setAttribute()   │
│    └─ rootData.themeMode 更新                   │
│                                                  │
│ 3. :root.light 或 :root.dark 選擇器生效        │
│    └─ CSS 變數重新應用                          │
│                                                  │
│ 4. 畫面更新，顯示對應主題色彩                  │
└─────────────────────────────────────────────────┘

用戶切換流程：
┌──────────────────────────────────────────────────┐
│ 1. 用戶點擊 ThemeModeRadioGroup 或 TestCodePanel │
│                                                   │
│ 2. 觸發 toggleTheme() 或 onSelect()              │
│    └─ Utils.setTheme(newTheme)                   │
│                                                   │
│ 3. HTML class / data-theme 屬性改變             │
│    └─ CSS 變數根據選擇器重新應用                │
│                                                   │
│ 4. 所有使用 CSS 變數的組件立即更新              │
└──────────────────────────────────────────────────┘

這專案的深淺模式運作方式如下：
核心機制
狀態 存在全局 reactive 物件 useRootStore.ts 的 rootData.themeMode，型別是 ThemeMode enum（"" / "light" / "dark"）。
切換核心 在 Utils.ts:868 的 setTheme()：

public static setTheme(theme: ThemeMode): void {
    // 移除舊 class
    if (rootData.themeMode != ThemeMode.NONE) {
        document.documentElement.classList.remove(rootData.themeMode);
    }
    document.documentElement.setAttribute("data-theme", theme);
    document.documentElement.classList.add(theme);
    rootData.themeMode = theme;
}
也就是在 <html> 根元素上同時加 class（light/dark）和 data-theme 屬性。
CSS 層面
用 CSS 變數搭配根選擇器切換，定義在 tailwind-tokens-output.css：
* 深色是預設（:root 直接定義）
* 淺色 用 :root.light {} 覆蓋同名變數
所有元件透過 tw: 前綴的顏色變數（如 tw:bg-black-2）取值，因此切換 <html> 的 class 後，變數重新解析，畫面即時更新，不需重繪元件。
進入點
* 初始化：useRootView.ts:687 登入後依後端 bgInfo.theme 呼叫 setTheme()
* UI 切換：ThemeModeRadioGroup.vue（系統設定裡的淺色/深色選項）、手機版在 DialogMobileSystemSetting.vue
* 封裝：useSystemSetting.ts 提供 currentTheme 這個可讀寫的 computed，get 讀 rootData.themeMode、set 呼叫 Utils.setTheme()
流程總結
setTheme() → 改 <html> 的 class/data-theme → :root.light 選擇器生效 → CSS 變數重新套用 → 用到變數的元件自動更新。
要我深入看某一塊（例如新增主題色、或某元件怎麼吃這些變數）嗎？



——
是的，主要就是 <html lang> 的關係。
驗證邏輯很簡單：那三行文字是完全一樣的漢字、同樣的 CSS、同樣的 font-family，唯一在切換語系時改變的變數就是 i18n/index.ts:68 把 document.documentElement.lang 改掉了。既然文字和樣式都沒變、只有 lang 變、結果字形就變——那變數就是 lang。
機制是：漢字落到系統 fallback 字型（-apple-system / sans-serif）時，瀏覽器依 lang 挑地區字形（Han Unification），ja 給日文字形、zh-CN 給簡中字形。
你可以自己快速驗證：開 DevTools，把 <html> 的 lang 手動從 ja-JP 改成 zh-TW，那三行「籌碼設置」字形會立刻跟著變，但頁面其他東西都不動——這樣就百分百確定是 lang 造成的。
唯一的但書：如果哪天改成有明確載入、且完整涵蓋這些字的 CJK web font（不走系統 fallback），那 lang 就不會再影響字形。目前專案是走系統 fallback，所以 lang 就是主因。


修改密碼功能會搭配 vee-validate zod 

專案的圖片加上 hash



2026/07/02
Ver-validate 和相依驗證套件的 zod 他會有相依性版本的問題 peerDependency 他相依是 3.x 但是 zod 已經到 4.x 所以裝起來會有問題，那目前就是用 yup 

專案的直橫版是用 usParser 根據 user-agent 去判斷是直還是橫

Codex Claude cursor 都有用

時區的內容，UTC +8 / +n 等等

1. 修正財務查詢的日期範圍問題 修正信用廳查財務資料時，日期篩選範圍不正確，導致某些 12 點後的下注資料查不到。 這次主要調整了財務查詢的時間計算邏輯，讓系統在北京時間 / 美東時間之間切換時，顯示時間和實際查詢時間可以對得上。 也同步修正了下注紀錄、交易紀錄、會員報表列表上的日期顯示，避免畫面時間和查詢結果不一致。 另外也把會員報表的時區選擇放進搜尋條件裡，避免切換 tab 之後時區被重置。
2. 調整財務查詢的快速日期選項 中文語系的日期快捷選項改成比較符合需求的： 今日 / 昨日 / 本週 / 上週 也補上「上週」的計算邏輯，範圍是上週一到上週日。 非中文語系則維持比較通用的： 今日 / 7 天 / 30 天
3. 修改密碼表單驗證套件從 zod 換成 yup 修改密碼功能原本使用 zod 做表單驗證，這次改成 yup。 同時調整錯誤訊息邏輯，讓必填、密碼格式錯誤、確認密碼不一致時，可以顯示專案裡原本的多語系文案，而不是套件預設的英文錯誤訊息。
另外今天進到分支的內容還有：
* 新增修改密碼功能
* 調整籌碼圖片檔名與路徑
* 籌碼資源從 SVG 改成 PNG
* 修正財務查詢裡金額字體大小不一致
* 移除沒用到的資源
如果要一句話總結今天的重點，可以寫：
今日主要完成財務查詢日期與時區邏輯修正，補齊中文日期快捷選項，並調整修改密碼表單驗證套件與錯誤訊息顯示。




2026/07/03
1. 修改密碼的錯誤訊息，是放在下面不標示紅匡
    但是正常來說應該是每一個 input 都一個紅匡
    不然就是在上方有一個大區塊是一個紅匡



2026/07/03
籌碼太大要做 preload



2026/07/06
修改密碼介面  如果把 new 出來的 object 的 function assign 到另外一個 variable 裡面，那在掉用裡面的 function 時 this 會失效

打 api 遇到錯誤訊息時，可以在打完 api 之後統一發送到一個接口 showWarning 去做顯示 或是


2026/07/07
Vite 的功用可以在 prod 移除全站 log
Git flow 原則
Regex 的 hookhead 原則，可以檢查字串裡面是否有指定的字串


2026/07/09
調整內陰影
Loading icon 會旋轉

查帳頁面 以前的限制是只能鎖定兩週，但是之前說開到一個月，但是再送了一個月日期條件之後，他送回來的資料還是只有兩個禮拜，但是公司只有前端沒有後端，所以當時花了一些時間在 check 前端的邏輯，但是在找了一段時間之後還是確認沒有問題，才往上呈報，最後才知道說是後端的問題




2026/07/13

縮字會有一些問題，如果有一些地方沒有要縮字，特別是同一排
但有一塊要縮字那就會造成字體大小不一樣，你就只能針對這一塊客製化縮字方式
這樣的話你就會先有一塊消耗在

Git 跟別人衝突時，可以先確認一下自己跟別人修改的部分有沒有重疊


        :is(.tab-list[data-item-count="2"]) & {
            margin-left: 90px;
        }
        :is(.tab-list[data-item-count="3"]) & {
            margin-left: 20px;
            max-width: 210px;
            white-space: wrap;
            align-self: flex-end;
        }
        :is(.tab-list[data-item-count="4"]) & {
            margin-left: 20px;
            max-width: 150px;
            white-space: wrap;
            /* align-self: flex-end; */
        }



        :is(.tab-list[data-item-count]) &:first-child {
            margin-left: 0;
        }



屬性選擇器 增加權重，搭配 :is 

@font-face {
    font-family: Inter;
    font-style: normal;
    font-weight: 100 900;
    unicode-range: U+0020-036F, U+0400-0457, U+04AE-04AF, U+04E8-04E9, U+1E00-1EFF, U+2000-202F, U+20A0-20BF, U+2212, U+2713;
    src:
        url(/assets/font/interroman-3e0fa3aadc.woff2) format("woff2 supports variations"),
        url(/assets/font/interroman-3e0fa3aadc.woff2) format("woff2-variations");
}

字體怎麼 preload


2026/07/14

設計的內容經常性會不置中，但是通常來說還是希望照他們的排，原因只是希望先都照他們的如果看起來不對再說

查帳頁面，有四個 tab 可以查不同的報表，其中他們會有時間和快捷鍵的搜尋條件
在切換時沒有說是否連動，在很久之後才開 issue 出來，說是要拆開



2026/07/15
1. Dialog 快速開啟和關閉造成的問題

總結：Panel Dialog 快速開關卡死問題
問題現象
使用 vue-final-modal（vfm 4.5.5）的 dynamic modal（useModal）時，快速開啟→關閉會讓 dialog 卡住、之後再也打不開。
根本原因（在 vfm 這一層）
open() / close() 只是同步翻一個布林值，但真正的掛載/卸載是靠 CSS transition 事件非同步驅動，兩者步調不一致：
1. open() → modelValue=true、push 進 dynamicModals、enter 動畫開始
2. 動畫還沒跑完就 close() → leave 動畫中斷了 enter
3. vfm 「從 dynamicModals 移除」這動作只在 leave 動畫播完、觸發 closed 事件時才執行；被中斷時 closed 不會發 → modal 殘留在陣列裡沒被清掉
4. 下次 open() 時 modelValue 已是 false，於是又 push 一次 → 陣列出現兩筆同 id（Vue :key 衝突）+ 殘留實例 transition 狀態卡死 → 畫面打不開
一句話：leave 中斷 enter → closed 沒發 → modal 沒被清出陣列 → 下次 open 產生重複 key 的殭屍實例。
目前採用的解法（DialogMobilePanelLayout.vue）
利用這個 panel 的兩段式關閉特性從源頭避免中斷：
* 關閉不是直接呼叫 vfm close()，而是先播 .panel-layout-section 的滑出動畫（200ms），等它的 transitionend 才 emits("notifyVfmToClose") → 上層才真的關 vfm。這讓「真正的 vfm 關閉」天生延後約 200ms，此時 vfm 的 opened 已完成，不會被中斷。
* 加一個 isContentEnterStarted 旗標：
    * onTransitionStart（進場 transform 一開始移動）→ 設為 true
    * closePanel() 開頭 if (!isContentEnterStarted.value) return —— 只擋開啟的第 0 幀（panel 還沒位移的瞬間，此時反向會因無位移而不觸發 transitionend → 關不掉），一旦開始移動就允許從當前位置平滑反向收合。
結果：開到一半可以順順地反向關閉，且不會回到 vfm 卡死的狀況。


2026/07/16 12:00 以前

美東時間 : 2026/07/15 00:00 ~ 2026/07/15 23:59
北京時間 : 2026/


財務報表時區調整總結(2026-07-16)
一、getToday — 一般 tab 的今天(依全域時區 flag)
useFinancialDate.ts

拿掉原本冗餘的 isBeijingDifferentDay 判斷,改由 toCurrentTimeZoneDate(內部 localeDateToTimeZoneDate 已依 bBeijingTimeZoom 決定 +12h/0)統一處理。
美東:當天 00:00:00 ~ 23:59:59。
北京:營業日以中午 12:00 為界,未過中午算前一天 → 前一天 12:00 ~ 今天 11:59:59(與舊版 FinancialPanel.setDate() 一致)。
二、新版會員報表 — 獨立的今天 getNewMemberReportToday
useFinancialDate.ts

新會員報表時區由選單(selectedTimeZone)決定,不套用北京 12:00 營業日邊界。
選北京:強制 +12h(toBeijingTimeZoneDate)轉成北京當天;選美東:直接用伺服器當天。兩者都是當天 00:00:00 ~ 23:59:59。
新增 resetNewMemberReportDate(timeZone):初始化 + 切換時區選單時都會把日期重設回「當天」再查詢(DialogMobileFinancial.onTimeZoneChange)。
三、送 API 的日期格式化 — 移除重複換算
useFinancialRecordMediator.ts

移除 formatFinancialReportDateString(它會對北京再 +12h 一次,造成「美東下午時 API 多送一天」的重複換算 bug)。
改為直接呼叫 export 出來的 formatDateString(date)——因為日期在 getNewMemberReportToday/日曆選取時已換算成目標時區當天,時區換算只做一次。顯示與 API 結果一致。
四、日曆 maxDate — 依報表類型分流
DialogMobileFinancial.vue

日曆傳入參數 today → 更名 maxDate(整條 mobile 鏈路 + 共用 hook useFinancialCalendar)。
傳入值改為「今天的結束時間」(getEndOfDay(...)),解決北京營業日跨天時少一天無法選取的問題。
新會員報表用 getEndOfDay(getNewMemberReportToday(選單時區)),其餘 tab 用 getEndOfDay(getToday())。
五、順手的冗餘清理
oneDayMs 改 export 共用(移除 mediator 重複定義)。
DateRangeType 移除多餘 export。
移除無人使用的回傳:closeFinancialCalendar、closeFinancialDialog、useFinancial 對外的 newMemberReportStartDate/EndDate。
⏳ 尚未處理(待辦)
formatDateString 的驗證:美東/北京 × 上午/下午 四種情境實測(先前只做過紙上推導)。
PC 端:PC 財務頁仍走舊 today + getToday(),北京少一天問題還在,未套用本次時區邏輯。
死 code 清理:PC 死檔 useDialogPCFinancialCalendar.ts + 一批舊 panel 財務元件(需刪前逐檔複驗)。
日曆 hook 重構:useFinancialCalendar 與 useFinancialCalendarPC 邏輯高度重複。
v-fit-input-text:measureSpan 漏同步 line-height,導致字體被提早縮小(籌碼設定 input)。

2026/07/20
今天在財務報表頁的內容上遇到問題
問了 QA 也還是無法釐清，主要是在非信用廳那邊，美東時間是查一整天
上禮拜說，信用廳那邊，非會員報表北京時間是查詢 12 ~ 12 會員報表則是北京時間是查詢 0 ~ 23:59:59 點，美東時間是一整天
但是後來說有需求修改，我問 QA 以為是信用廳的美東是 12 ~ 12 但北京變成是一整天

後來一直問才知道說美東和北京時間都是一整天，一整個被 QA 騙，而且問題點是在於這個主管跟 QA 討論時都沒有跟 RD 討論，最後看到實際的需求都是看的 issue 單才知道

而且今天又講了一個東西是也蠻扯的，就是在通用版那邊的時間，是查到 23:59:59，然後他又隨口說一句就是送出時間要到 12 點，因為後端那邊是這樣判斷的，這種東西應該要在開發之前就講了，不過說了之後大概又只會說要用 AI 去看通用版，反正自己都沒有責任
又看到這麼爛的主管真是的

11:59:59 的查詢
QA 討論沒有跟

2026/07/21
1. 確認財務報表的查詢時間
    北京時間是今天的 0 點到半夜，但是送出時間會是要轉成美東 12 ~ 12 
    美東時間則是 0 ~ 半夜
2. i18n 用 computed 去包裝的時候，就可以語系就可以響應
3. 時區的問題可以再多想一些
4. 選了北京時間，時間要變成是 + 12，美東時間是維持










